{
  "name": "puyo-scala-2",
  "version": "1.0.0",
  "description": "Puyo Puyo game implemented in Scala.js (Japanese code version)",
  "scripts": {
    "compile": "sbt fastOptJS",
    "build": "sbt fullOptJS",
    "test": "sbt test",
    "serve": "npx http-server . -p 8083 -o",
    "watch": "sbt ~fastOptJS",
    "dev": "concurrently \"npm run watch\" \"npm run serve\"",
    "clean": "sbt clean"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "http-server": "^14.1.1"
  }
}
